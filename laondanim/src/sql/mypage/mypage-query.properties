selectBoardCount=SELECT COUNT(*) FROM BOARD_TB
selectBoardPage=SELECT * FROM (SELECT A.*, ROWNUM AS RNUM FROM (SELECT * FROM BOARD_TB ORDER BY NO DESC) A) WHERE RNUM BETWEEN ? AND ?
selectBoard=SELECT * FROM BOARD_TB WHERE NO = ?