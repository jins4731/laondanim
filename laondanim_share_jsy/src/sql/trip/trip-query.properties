searchtriplist=SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB)A) WHERE R BETWEEN ? AND ?
searchtriplistkeyword=SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE TAG LIKE ?)A) WHERE R BETWEEN ? AND ?
totaldata=SELECT COUNT(*) FROM TRIP_TB
totaldatakeyword=SELECT COUNT(*) FROM TRIP_TB WHERE TAG LIKE ?
totaldatapr=SELECT COUNT(*) FROM TRIP_TB WHERE CATEGORY=?
totaldataprkeyword=SELECT COUNT(*) FROM (SELECT * FROM TRIP_TB WHERE CATEGORY=?) WHERE TAG LIKE ?
searchtriplistpr=SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE CATEGORY=? ORDER BY WRITE_DATE DESC)A) WHERE R BETWEEN ? AND ?
searchtriplistprkeyword=SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE CATEGORY=? AND TAG LIKE ? ORDER BY WRITE_DATE DESC)A) WHERE R BETWEEN ? AND ?
totaldatafilter=SELECT COUNT(*) FROM TRIP_TB WHERE CATEGORY=? AND TRAVLE_LOCALE=? AND TAG LIKE ?
searchtriplist2=SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE CATEGORY=? AND TRAVLE_LOCALE=? AND TAG LIKE ?)A) WHERE R BETWEEN ? AND ?
sortlistlike=SELECT * FROM (SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE CATEGORY='\uC5EC\uD589 \uC77C\uC815' AND TRAVLE_LOCALE='\uC11C\uC6B8' AND TAG LIKE '%\uC57C%')A) WHERE R BETWEEN 1 AND 10)B
JOIN (SELECT TRIP_NO, COUNT(*)AS COUNT FROM (SELECT * FROM TRIP_TB  JOIN LIKE_TB ON TRIP_TB.NO=LIKE_TB.TRIP_NO) GROUP BY TRIP_NO)C
ON B.NO=C.TRIP_NO ORDER BY COUNT DESC
sortlistwrite=SELECT * FROM (SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE CATEGORY=? AND TRAVLE_LOCALE=? AND TAG LIKE ?)A) WHERE R BETWEEN ? AND ?)B JOIN (SELECT TRIP_NO, COUNT(*)AS COUNT FROM (SELECT * FROM TRIP_TB  JOIN LIKE_TB ON TRIP_TB.NO=LIKE_TB.TRIP_NO) GROUP BY TRIP_NO)C ON B.NO=C.TRIP_NO ORDER BY WRITE_DATE DESC
sortlistwritelike=SELECT * FROM (SELECT * FROM (SELECT A.*, ROWNUM AS R FROM (SELECT * FROM TRIP_TB WHERE CATEGORY=? AND TRAVLE_LOCALE=? AND TAG LIKE ?)A) WHERE R BETWEEN ? AND ?)B JOIN (SELECT TRIP_NO, COUNT(*)AS COUNT FROM (SELECT * FROM TRIP_TB  JOIN LIKE_TB ON TRIP_TB.NO=LIKE_TB.TRIP_NO) GROUP BY TRIP_NO)C ON B.NO=C.TRIP_NO ORDER BY COUNT DESC, WRITE_DATE DESC
tagdatalist=SELECT TAG FROM TRIP_TB WHERE TAG LIKE ?